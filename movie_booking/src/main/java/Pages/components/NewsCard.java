package Pages.components;

import base.BasePage;
import org.openqa.selenium.WebDriver;

public class NewsCard extends BasePage {
    public NewsCard(WebDriver driver) {
        super(driver);
    }
}
