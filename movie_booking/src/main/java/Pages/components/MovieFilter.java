package Pages.components;

import base.BasePage;
import org.openqa.selenium.WebDriver;

public class MovieFilter extends BasePage {
    public MovieFilter(WebDriver driver) {
        super(driver);
    }
}
